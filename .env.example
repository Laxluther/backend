# ===================================
# WellNest Backend Environment Variables
# ===================================

# Required for Production - Must be set!
SECRET_KEY=your-super-secret-key-at-least-32-characters-long
JWT_SECRET_KEY=your-jwt-secret-key-different-from-secret-key

# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your-database-password
DB_NAME=ecommerce_db
DB_PORT=3306

# Production Database (Optional - overrides above in production)
PROD_DB_HOST=your-prod-db-host
PROD_DB_USER=your-prod-db-user
PROD_DB_PASSWORD=your-prod-db-password
PROD_DB_NAME=your-prod-db-name

# Redis Cache Configuration
REDIS_URL=redis://localhost:6379/0

# Email Configuration (Replace with your credentials)
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password

# URLs Configuration
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:5000

# Production URLs (Used when FLASK_ENV=production)
PROD_FRONTEND_URL=https://your-domain.com
PROD_BACKEND_URL=https://api.your-domain.com
CORS_ORIGINS=https://your-domain.com

# File Upload Configuration
PROD_UPLOAD_FOLDER=/path/to/production/uploads

# Database Connection Pool Settings (Optional)
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=3600

# Flask Environment
FLASK_ENV=development

# ===================================
# Security Notes:
# ===================================
# 1. SECRET_KEY and JWT_SECRET_KEY must be different and at least 32 characters
# 2. Never commit this file with real values to git
# 3. Use strong, unique passwords for database and email
# 4. In production, consider using environment-specific secret management
# ===================================