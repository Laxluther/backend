# ===================================
# PRODUCTION Environment Configuration
# ===================================
# Copy this to .env.production and fill in your production values
# NEVER commit .env.production to version control

# Flask Environment
FLASK_ENV=production

# CRITICAL SECURITY KEYS - Generate new ones for production
SECRET_KEY=CHANGE_THIS_TO_64_CHAR_RANDOM_STRING
JWT_SECRET_KEY=CHANGE_THIS_TO_64_CHAR_RANDOM_STRING_DIFFERENT_FROM_SECRET

# Production Database Configuration
DB_HOST=your_production_db_host
DB_USER=your_production_db_user
DB_PASSWORD=your_production_db_password
DB_NAME=your_production_db_name
DB_PORT=3306

# Redis Configuration (Production)
REDIS_URL=redis://your_redis_host:6379/0
SESSION_REDIS_URL=redis://your_redis_host:6379/1

# Email Configuration (Production)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your_production_email@gmail.com
MAIL_PASSWORD=your_app_specific_password
MAIL_DEFAULT_SENDER=your_production_email@gmail.com

# Email Features
EMAIL_VERIFICATION_REQUIRED=true
SEND_ORDER_EMAILS=true

# File Upload (Production)
MAX_CONTENT_LENGTH=33554432
UPLOAD_FOLDER=/var/www/uploads
PROD_UPLOAD_FOLDER=/var/www/uploads

# Backend Configuration (Production)
BACKEND_BASE_URL=https://api.yourdomain.com
HOST=0.0.0.0
PORT=5000

# Business Configuration
COMPANY_NAME=WellnessNest
COMPANY_EMAIL=info@wellnessnest.com
COMPANY_PHONE=+916261116108
BUSINESS_STATE_CODE=MP
BUSINESS_GSTIN=YOUR_PRODUCTION_GSTIN_NUMBER

# Delivery Settings
FREE_DELIVERY_THRESHOLD=500
STANDARD_DELIVERY_CHARGE=50
EXPRESS_DELIVERY_CHARGE=100

# Referral System
REFERRAL_REWARD_AMOUNT=50
REFERRAL_MIN_ORDER_AMOUNT=500

# Payment Gateway (Razorpay Production)
RAZORPAY_KEY_ID=your_production_razorpay_key_id
RAZORPAY_KEY_SECRET=your_production_razorpay_key_secret

# CORS Origins (Production domains only)
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# ===================================
# Security Notes:
# ===================================
# 1. Generate unique SECRET_KEY and JWT_SECRET_KEY (64+ characters each)
# 2. Use strong database passwords (32+ characters)
# 3. Use app-specific passwords for email
# 4. Restrict CORS_ORIGINS to your actual domain
# 5. Set up SSL/HTTPS for all production URLs
# ===================================